{
  "name": "pxladmin-redesign",
  "private": true,
  "version": "0.0.0",
  "description": "A starter project for AngularJS",
  "repository": "https://github.com/teamfora/pxladmin-redesign",
  "license": "MIT",
  "dependencies": {
    "express": "~4.1.1"
  },
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "node server.js",

    "pretest": "npm install"
  },
  "engines": {
    "node": "0.10.x"
  }
}
